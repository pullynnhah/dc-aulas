const sumOneLiner = (num1, num2) => num1 + num2;

const sum = (num1, num2) => {
  return num1 + num2;
};

const greet = () => "Hello, 🌎!";
const doubler = num => 2 * num;

console.log(`sumOneLiner(3,7) ~> ${sumOneLiner(3, 7)}`);
console.log(`sum(3,7) ~> ${sum(3, 7)}`);
console.log();

console.log(greet());
console.log(doubler(26));
