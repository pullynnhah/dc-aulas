// Escreva um programa onde, você chame uma função enviando um número, a
// função deve imprime na tela os número de 1 até o número que você enviou

function logNumbers(max) {
  for (let i = 1; i <= max; i++) console.log(i);
}

logNumbers(10);
